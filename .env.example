# Documentation template only; scripts do NOT automatically load this file.
# Set these as environment variables in your local shell. Never commit real values.
PGHOST=localhost
PGPORT=5432
PGDATABASE=quant
PGUSER=postgres
# PGPASSWORD deliberately omitted: an interactive prompt is the default.
# Optional: use your own local data folders (do not publish personal paths).
# FUTURES_WORK_ROOT=
# FUTURES_STATEMENTS_DIR=
# FUTURES_MARKET_DIR=

